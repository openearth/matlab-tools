function locationsets=ddb_initializeDelft3DWAVELocationSet(locationsets,iac)

locationsets(iac).x=[];
locationsets(iac).y=[];
locationsets(iac).handle=[];
locationsets(iac).activepoint=1;
locationsets(iac).nrpoints=0;
locationsets(iac).pointtext={''};
